---
phase: 09-explore-changelog-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/explore.md
  - src/templates/EXPLORATION.md
  - CHANGELOG.md
  - README.md
  - package.json
  - .teddy/PROJECT.md
autonomous: true
team:
  name: teddy-09-01
  size: 3
  parallel_waves: 1
  roles:
    - name: explore-dev
      domain: "Explore skill redesign and exploration template improvements"
      owns: ["src/commands/explore.md", "src/templates/EXPLORATION.md"]
    - name: changelog-dev
      domain: "CHANGELOG creation and release notes system"
      owns: ["CHANGELOG.md", "README.md"]
    - name: release-dev
      domain: "Version bump, project state sync, and test verification"
      owns: ["package.json", ".teddy/PROJECT.md"]
---

<objective>
## Goal
Improve the explore skill with better context loading, past-exploration awareness, and structured parallel analysis. Create CHANGELOG.md with complete version history. Bump to v0.7.0 and sync all project metadata.

## Purpose
Close the v0.7 milestone with a more effective explore workflow, proper release documentation, and consistent versioning across all project files.

## Output
- Improved `src/commands/explore.md` with enhanced proactive capabilities
- Updated `src/templates/EXPLORATION.md` with prior-exploration field
- New `CHANGELOG.md` with v0.1.0–v0.7.0 history
- README.md updated with changelog reference
- package.json bumped to 0.7.0
- PROJECT.md synced with v0.7.0 requirements
</objective>

<context>
## Project Context
@.teddy/PROJECT.md
@.teddy/ROADMAP.md
@.teddy/STATE.md

## Source Files
@src/commands/explore.md
@src/templates/EXPLORATION.md
@README.md
@package.json

## Exploration
@.teddy/explorations/EXPLORATION-2026-03-25-polish-automation.md
</context>

<acceptance_criteria>

## AC-1: Explore skill loads .teddy/ state in context section
```gherkin
Given the explore.md skill file
When the <context> section is read
Then it includes @.teddy/PROJECT.md, @.teddy/ROADMAP.md, and @.teddy/STATE.md references
And these are auto-loaded when the skill is invoked
```

## AC-2: Explore reads past explorations
```gherkin
Given a project with existing .teddy/explorations/*.md files
When /teddy:explore runs the validate_teddy_state step
Then it reads past exploration files to understand what was already investigated
And avoids re-surfacing known issues already addressed
```

## AC-3: Explore has structured parallel analysis
```gherkin
Given the analyze_codebase step
When it describes how to run analyses
Then each analysis has a clear instruction for parallel execution via Explore subagents
And the step explicitly lists what each subagent investigates
```

## AC-4: CHANGELOG exists with complete history
```gherkin
Given the CHANGELOG.md file at project root
When a user reads it
Then it lists all versions from v0.1.0 through v0.7.0
And each version has date, summary, and categorized changes (Added, Changed, Fixed)
And it follows Keep a Changelog format
```

## AC-5: Version is 0.7.0 everywhere
```gherkin
Given the version bump is complete
When checking package.json, PROJECT.md
Then all show version 0.7.0
And all tests pass
```

## AC-6: README references CHANGELOG
```gherkin
Given the README.md file
When a user reads it
Then there is a Changelog section or link pointing to CHANGELOG.md
```

</acceptance_criteria>

<tasks>

<!-- explore-dev tasks (6 tasks) -->
<task type="auto" role="explore-dev" wave="1">
  <name>Task 1: Add .teddy/ state files to explore.md context section</name>
  <files>src/commands/explore.md</files>
  <action>
    In the `<context>` section (currently only `@context/session.md`), add:
    ```
    @.teddy/PROJECT.md
    @.teddy/ROADMAP.md
    @.teddy/STATE.md
    ```
    This ensures state files are auto-loaded when the skill is invoked,
    rather than relying on step 1 to read them manually.
    Keep @context/session.md as well — it provides session runtime context.
  </action>
  <verify>Read explore.md and confirm context section has all 4 references.</verify>
  <done>AC-1 satisfied: explore context includes .teddy/ state references</done>
</task>

<task type="auto" role="explore-dev" wave="1">
  <name>Task 2: Add past-exploration awareness to validate_teddy_state</name>
  <files>src/commands/explore.md</files>
  <action>
    In the `validate_teddy_state` step, after reading PROJECT.md, ROADMAP.md, STATE.md,
    add a sub-step:

    ```
    Also read existing explorations to avoid re-discovering known issues:
    - Glob for `.teddy/explorations/EXPLORATION-*.md`
    - For each found, read the title, date, and recommendation section
    - Store as **prior explorations** — skip re-surfacing findings that were already
      addressed in completed phases
    ```

    This prevents explore from repeatedly suggesting the same improvements that were
    already planned and executed.
  </action>
  <verify>Read explore.md validate_teddy_state step and confirm past-exploration reading is included.</verify>
  <done>AC-2 satisfied: explore reads past explorations</done>
</task>

<task type="auto" role="explore-dev" wave="1">
  <name>Task 3: Restructure analyze_codebase for parallel subagent execution</name>
  <files>src/commands/explore.md</files>
  <action>
    In the `analyze_codebase` step, the current 5 analyses are listed but lack
    structure for parallel execution. Rewrite to explicitly use Explore subagents:

    ```
    Spawn 3 parallel Explore subagents for analysis:

    **Subagent 1: Code Health**
    - Grep for TODO, FIXME, HACK, XXX across the codebase
    - Look for large files (>500 lines), duplicated patterns
    - Check for missing error handling at system boundaries

    **Subagent 2: Test & Dependency Health**
    - Check which modules have tests and which don't
    - Run `npm outdated` or equivalent for dependency health
    - Identify untested critical paths

    **Subagent 3: Roadmap vs Reality**
    - Cross-reference ROADMAP phases marked "complete" against actual code
    - Verify modules/files listed in SUMMARY.md actually exist
    - Check for empty or stub implementations
    - Compare with prior explorations to identify new vs known issues
    ```

    Keep the SECURITY note about not including secrets.
    Use the Agent tool with subagent_type="Explore" for each.
  </action>
  <verify>Read explore.md analyze_codebase step and confirm 3 structured subagent definitions.</verify>
  <done>AC-3 satisfied: analyze_codebase has structured parallel subagent instructions</done>
</task>

<task type="auto" role="explore-dev" wave="1">
  <name>Task 4: Improve discuss_and_refine with clearer round mechanics</name>
  <files>src/commands/explore.md</files>
  <action>
    In the `discuss_and_refine` step, improve the round tracking to be more concrete:

    1. Replace "spawn Explore agents for deep research" (line 131) with:
       "If deeper analysis needed, spawn an Explore subagent with a focused prompt
       targeting the selected topic. Present results before the next round."

    2. Add explicit round output format for rounds 1-2 (currently only round 3
       has a format block):
       ```
       ────────────────────────────────────────
       ROUND [N]/3
       ────────────────────────────────────────

       Updated findings based on your feedback:
       [refined table or focused analysis]

       What's your direction?
       - Narrow further
       - Approve to proceed
       - One more round
       ```

    3. Keep the existing round 3 convergence format unchanged.
  </action>
  <verify>Read explore.md discuss_and_refine step and confirm round 1-2 output format exists.</verify>
  <done>AC-2 partial: explore discussion flow improved</done>
</task>

<task type="auto" role="explore-dev" wave="1">
  <name>Task 5: Add code block language tags to explore.md</name>
  <files>src/commands/explore.md</files>
  <action>
    Review all code blocks in explore.md and add appropriate language tags:
    - ASCII art / status displays → ```text
    - Bash commands → ```bash
    - Markdown examples → ```markdown
    - File paths / generic → ```text

    Do NOT change any content — only add language tags to unlabeled ``` blocks.
  </action>
  <verify>Grep for bare ``` (without language tag) in explore.md — should find only closing ``` tags.</verify>
  <done>AC-3 partial: code blocks consistently labeled</done>
</task>

<task type="auto" role="explore-dev" wave="1">
  <name>Task 6: Update EXPLORATION.md template with prior-exploration field</name>
  <files>src/templates/EXPLORATION.md</files>
  <action>
    In the EXPLORATION.md template, add a new field after the frontmatter header fields:

    After `**Decision it informs:**`:
    ```
    **Prior explorations reviewed:** [List of EXPLORATION files checked, or "None — first exploration"]
    ```

    Also add this field to the Field Documentation table at the bottom:
    ```
    | `[Prior explorations]` | prose | Yes | Which past explorations were reviewed | `EXPLORATION-2026-03-24-stabilization.md, EXPLORATION-2026-03-25-flows.md` |
    ```

    This ensures every exploration documents what was already investigated,
    creating a traceable chain.
  </action>
  <verify>Read EXPLORATION.md template and confirm the new field exists in both the template and the field documentation table.</verify>
  <done>AC-2 partial: exploration template supports prior-exploration tracking</done>
</task>

<!-- changelog-dev tasks (5 tasks) -->
<task type="auto" role="changelog-dev" wave="1">
  <name>Task 7: Create CHANGELOG.md with complete version history</name>
  <files>CHANGELOG.md</files>
  <action>
    Create CHANGELOG.md at project root following Keep a Changelog format
    (https://keepachangelog.com/en/1.1.0/).

    Use git log and .teddy/ROADMAP.md to compile accurate entries.
    Run `git log --oneline` to see actual commit history.

    Structure:
    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

    ## [0.7.0] - 2026-03-25

    ### Added
    - CHANGELOG.md with complete version history
    - Improved explore skill with past-exploration awareness and parallel analysis
    - Install listing parity test to catch missing commands

    ### Changed
    - Explore skill redesigned with structured subagent analysis
    - EXPLORATION.md template includes prior-exploration tracking field

    ### Fixed
    - Install.js console listing now shows all 14 commands (was missing amend-plan, rollback, flows)
    - Markdown formatting consistency across all command and framework files
    - Code block language tags added throughout documentation

    ## [0.6.0] - 2026-03-25

    ### Added
    - Skill dependency management with auto-discovery and confirmation (/teddy:flows)
    - skill-flows.md framework document
    - FLOWS.md template for skill configuration
    - Loop integration: detect in PLAN, verify in APPLY, audit in UNIFY

    ## [0.5.0] - 2026-03-25

    ### Added
    - Plan amendment mid-execution (/teddy:amend-plan)
    - Rollback post-UNIFY with safety tags (/teddy:rollback)
    - Vitest upgraded to v4.x

    ### Changed
    - README.md completed with full documentation
    - Version bump and state file synchronization

    ## [0.4.0] - 2026-03-25

    ### Changed
    - Published to npm as teddy-framework

    ## [0.3.0] - 2026-03-25

    ### Added
    - Structured planning and phase decomposition (/teddy:plan)
    - Agent team orchestration with shared task lists (/teddy:apply)
    - Session resumption and state continuity (/teddy:resume)
    - Debug and review workflows (/teddy:debug, /teddy:review)
    - Cleanup command for orphaned teams (/teddy:cleanup)
    - Unify workflow for merging results (/teddy:unify)
    - Status display (/teddy:status)

    ### Changed
    - Skill metadata with descriptions in frontmatter
    - Colon separator syntax (/teddy:command)
    - Installer hardening with input validation and security warnings

    ## [0.2.0] - 2026-03-24

    ### Added
    - Codebase mapping templates (/teddy:map-codebase)
    - 7 structured codebase analysis documents

    ## [0.1.0] - 2026-03-24

    ### Added
    - Initial release
    - Skill-based architecture with slash commands
    - npx installer (teddy-framework)
    - Project initialization (/teddy:init)
    - Explore workflow (/teddy:explore)
    ```

    Verify entries against git log and ROADMAP.md for accuracy.
    Adjust dates if git history shows different dates.
  </action>
  <verify>Read CHANGELOG.md and confirm all versions present with categorized entries.</verify>
  <done>AC-4 satisfied: CHANGELOG.md with complete history</done>
</task>

<task type="auto" role="changelog-dev" wave="1">
  <name>Task 8: Add Changelog section to README.md</name>
  <files>README.md</files>
  <action>
    Add a "Changelog" section to README.md before the "License" section:

    ```markdown
    ## Changelog

    See [CHANGELOG.md](CHANGELOG.md) for a detailed list of changes in each version.
    ```

    Keep it minimal — one line with a link. The CHANGELOG.md has all the details.
  </action>
  <verify>Read README.md and confirm Changelog section exists before License.</verify>
  <done>AC-6 satisfied: README references CHANGELOG</done>
</task>

<task type="auto" role="changelog-dev" wave="1">
  <name>Task 9: Verify CHANGELOG accuracy against git history</name>
  <files>CHANGELOG.md</files>
  <action>
    Run `git log --oneline --all` and `git tag -l` to cross-reference:
    1. Verify each version's date matches actual commit dates
    2. Verify major features listed match actual commits
    3. Verify no significant changes are missing

    Fix any inaccuracies found. The CHANGELOG should be a reliable source of truth.
  </action>
  <verify>Run `git log --oneline` and compare with CHANGELOG entries — no major gaps.</verify>
  <done>AC-4 partial: CHANGELOG verified against git history</done>
</task>

<task type="auto" role="changelog-dev" wave="1">
  <name>Task 10: Add changelog update convention as comment in CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
    At the very top of CHANGELOG.md (after the title and format note), add a brief
    convention note as an HTML comment (invisible to readers but visible to editors):

    ```html
    <!-- Convention: Update this file during /teddy:unify for each plan completed.
         Use categories: Added, Changed, Deprecated, Removed, Fixed, Security.
         Keep [Unreleased] section at top for in-progress work. -->
    ```

    Also add an [Unreleased] section above the latest version for future changes:
    ```markdown
    ## [Unreleased]
    ```
  </action>
  <verify>Read top of CHANGELOG.md and confirm convention comment and [Unreleased] section exist.</verify>
  <done>AC-4 partial: CHANGELOG has update conventions</done>
</task>

<task type="auto" role="changelog-dev" wave="1">
  <name>Task 11: Verify README command table is complete and accurate</name>
  <files>README.md</files>
  <action>
    Read README.md and verify:
    1. The command table lists all 14 commands (check against src/commands/ directory)
    2. Descriptions match the frontmatter descriptions in each command file
    3. The "Framework Files" tree matches actual directory layout
    4. No broken formatting from Phase 08 changes

    Fix any discrepancies found.
  </action>
  <verify>Compare README command table with `ls src/commands/` output — all commands present.</verify>
  <done>AC-6 partial: README fully accurate</done>
</task>

<!-- release-dev tasks (5 tasks) -->
<task type="auto" role="release-dev" wave="1">
  <name>Task 12: Bump version to 0.7.0 in package.json</name>
  <files>package.json</files>
  <action>
    Update the "version" field in package.json from "0.6.0" to "0.7.0".
    Do NOT modify any other fields.
  </action>
  <verify>Run `node -e "console.log(require('./package.json').version)"` — should output "0.7.0".</verify>
  <done>AC-5 partial: package.json version is 0.7.0</done>
</task>

<task type="auto" role="release-dev" wave="1">
  <name>Task 13: Update PROJECT.md version and validated requirements</name>
  <files>.teddy/PROJECT.md</files>
  <action>
    In .teddy/PROJECT.md:

    1. Update Current State table:
       - Version: 0.7.0 (was 0.6.0)

    2. Add to Validated (Shipped) list:
       - `- [x] Markdown polish and documentation consistency — v0.7.0`
       - `- [x] Explore skill redesign with parallel analysis — v0.7.0`
       - `- [x] CHANGELOG and release notes system — v0.7.0`

    3. In the Active (In Progress) section, keep as:
       - `- [None — next milestone not started]`

    4. In Planned (Next) section:
       - Remove "External project validation" (already done per user confirmation)
       - Keep "CI/CD and release automation"

    5. Update Last Updated date to 2026-03-25
  </action>
  <verify>Read PROJECT.md and confirm version is 0.7.0 and requirements are updated.</verify>
  <done>AC-5 partial: PROJECT.md synced with v0.7.0</done>
</task>

<task type="auto" role="release-dev" wave="1">
  <name>Task 14: Verify package.json metadata is accurate</name>
  <files>package.json</files>
  <action>
    Read package.json and verify all metadata is accurate:
    1. Version is 0.7.0
    2. Description matches the project's current scope
    3. Keywords are relevant and complete
    4. Repository URL is correct
    5. Engine requirement is still valid (node >= 16.7.0)
    6. Files array includes all necessary directories (bin/, src/)

    Fix any metadata that is outdated or inaccurate.
  </action>
  <verify>Read package.json and confirm all fields are accurate.</verify>
  <done>AC-5 partial: package.json metadata verified</done>
</task>

<task type="auto" role="release-dev" wave="1">
  <name>Task 15: Run full test suite and verify green</name>
  <files>test/install.test.js, test/commands.test.js</files>
  <action>
    Run `npx vitest run` to execute all tests.
    Verify all tests pass with the version bump and any other changes.
    If any test fails, investigate and fix the issue.

    Specifically check:
    - Install parity test still passes (new commands haven't broken it)
    - Command structure tests still pass (all 15 command files valid)
    - All utility function tests pass
  </action>
  <verify>Run `npx vitest run` — output shows all tests passing, zero failures.</verify>
  <done>AC-5 satisfied: all tests pass with v0.7.0</done>
</task>

<task type="auto" role="release-dev" wave="1">
  <name>Task 16: Verify version consistency across all files</name>
  <files>package.json, .teddy/PROJECT.md</files>
  <action>
    Cross-check that version 0.7.0 appears consistently:
    1. package.json → "version": "0.7.0"
    2. .teddy/PROJECT.md → Version: 0.7.0

    Grep the codebase for "0.6.0" to find any stale version references
    that should have been updated to 0.7.0.

    Do NOT modify files owned by other roles. If stale references are found
    in other files, report them via SendMessage to the appropriate teammate.
  </action>
  <verify>Run `grep -r "0.6.0" --include="*.json" --include="*.md" .` — should return zero results (or only in CHANGELOG/ROADMAP history).</verify>
  <done>AC-5 satisfied: version 0.7.0 consistent everywhere</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- .teddy/STATE.md — managed by lead after UNIFY
- .teddy/ROADMAP.md — managed by lead after UNIFY
- .teddy/phases/ — historical plan and summary files
- .teddy/explorations/ — exploration outputs
- bin/install.js — no changes needed this phase
- test/ — test files only modified if failures found
- src/commands/ (except explore.md) — completed in Phase 08
- src/frameworks/ — completed in Phase 08

## SCOPE LIMITS
- Explore redesign is structural improvement, not a rewrite
- CHANGELOG entries must be factual, verified against git history
- Version bump is 0.7.0 only — no npm publish
- Do NOT add new commands or features

## ROLE ISOLATION (enforced by team.roles.owns)
- explore-dev owns: src/commands/explore.md, src/templates/EXPLORATION.md
- changelog-dev owns: CHANGELOG.md, README.md
- release-dev owns: package.json, .teddy/PROJECT.md
- No cross-ownership modifications without SendMessage coordination

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` passes all tests
- [ ] explore.md has .teddy/ state in context + past-exploration awareness + parallel subagent instructions
- [ ] CHANGELOG.md exists with v0.1.0–v0.7.0 entries
- [ ] README.md has Changelog section
- [ ] package.json version is 0.7.0
- [ ] PROJECT.md version is 0.7.0 with updated requirements
- [ ] No stale 0.6.0 references remain
</verification>

<success_criteria>
- All 3 role-specialists completed their tasks
- All verification checks pass
- All worktrees ready for merge
- v0.7.0 milestone fully delivered
</success_criteria>

<output>
After completion, create `.teddy/phases/09-explore-changelog-release/09-01-SUMMARY.md`
</output>
