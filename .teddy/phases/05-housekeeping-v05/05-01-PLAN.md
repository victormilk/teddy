---
phase: 05-housekeeping-v05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .teddy/PROJECT.md
  - .teddy/STATE.md
  - .teddy/ROADMAP.md
  - README.md
  - package.json
  - package-lock.json
autonomous: true
team:
  name: teddy-05-01
  size: 3
  parallel_waves: 1
  roles:
    - name: state-sync
      domain: "Project state files — version alignment, milestone tracking, session continuity"
      owns: [".teddy/PROJECT.md", ".teddy/STATE.md", ".teddy/ROADMAP.md"]
    - name: docs-dev
      domain: "User-facing documentation — README completeness"
      owns: ["README.md"]
    - name: deps-eng
      domain: "Dependencies and package configuration"
      owns: ["package.json", "package-lock.json"]
---

# Phase 05 Plan 01: Housekeeping v0.5

**Bump para v0.5.0, sincronizar state files, completar documentação, e atualizar dependências.**

## Objective

- **Goal:** Bump package.json para v0.5.0, todos os state files refletem v0.5.0, ROADMAP tem milestone v0.5 definido, README está completo, vitest atualizado para 4.x
- **Purpose:** v0.4.0 já publicada — avançar para v0.5.0 com state files limpos antes de iniciar features novas
- **Output:** 6 arquivos atualizados, testes green com vitest 4.x

## Acceptance Criteria

<ac id="AC-1">

**Version Consistency**
```gherkin
Given PROJECT.md, package.json, and ROADMAP.md
When I check the version field in each
Then all reference v0.5.0 consistently
```
</ac>

<ac id="AC-2">

**ROADMAP Has v0.5 Milestone**
```gherkin
Given ROADMAP.md
When I read the milestones section
Then v0.5 "Housekeeping + Plan Resilience" exists with phases 05 and 06 defined
And v0.3 and v0.4 are marked as complete
```
</ac>

<ac id="AC-3">

**STATE.md Reflects Current Position**
```gherkin
Given STATE.md
When I read current position
Then milestone shows v0.5 Housekeeping + Plan Resilience
And phase shows 05 — Housekeeping v0.5
And no stale v0.3 or v0.4 references remain in current position
```
</ac>

<ac id="AC-4">

**README Lists All Commands**
```gherkin
Given README.md commands table
When I count the listed commands
Then /teddy:cleanup is present in the table
And all 11 commands from teddy.md are listed
```
</ac>

<ac id="AC-5">

**vitest 4.x With Green Tests**
```gherkin
Given vitest upgraded to ^4.0.0 in package.json
When I run `npm test`
Then all tests pass
And no deprecation warnings appear
```
</ac>

## Tasks

<!-- state-sync tasks -->
<task type="auto" role="state-sync" wave="1">
  <name>Task 1: Update PROJECT.md version to v0.5.0</name>
  <files>.teddy/PROJECT.md</files>
  <action>
    Change version from 0.3.0 to 0.5.0 in the Current State table.
    Update Last Updated to 2026-03-25.
    Update "Active (In Progress)" requirements to reflect v0.5 focus:
    - Move "Structured planning and phase decomposition" to Validated
    - Move "Agent team orchestration with shared task lists" to Validated
    - Add "Plan amendment mid-execution" to Active
    - Add "Rollback pós-UNIFY with safety tags" to Active
  </action>
  <verify>grep "0.5.0" .teddy/PROJECT.md && grep "Plan amendment" .teddy/PROJECT.md</verify>
  <done>AC-1 satisfied: PROJECT.md shows v0.5.0</done>
</task>

<task type="auto" role="state-sync" wave="1">
  <name>Task 2: Update ROADMAP.md to v0.5 milestone</name>
  <files>.teddy/ROADMAP.md</files>
  <action>
    Update Current Milestone to: **v0.5 Housekeeping + Plan Resilience** (v0.5.0)
    Status: In Progress, Phases: 0 of 2 complete.

    Add v0.4 as a completed milestone (published, no phases tracked in Teddy).
    Keep v0.3 section intact as complete.

    Update v0.5 phases table:
    | Phase | Name | Plans | Status | Completed |
    | 05 | Housekeeping v0.5 | 1 | In Progress | — |
    | 06 | Plan Resilience | TBD | Planned | — |

    Update Phase Details for 05 and 06 to reference v0.5.
    Update "Last updated" to 2026-03-25.
  </action>
  <verify>grep "v0.5" .teddy/ROADMAP.md && grep "Phase 05" .teddy/ROADMAP.md && grep "Phase 06" .teddy/ROADMAP.md</verify>
  <done>AC-2 satisfied: ROADMAP has v0.5 milestone with both phases</done>
</task>

<task type="auto" role="state-sync" wave="1">
  <name>Task 3: Reset STATE.md for v0.5 milestone</name>
  <files>.teddy/STATE.md</files>
  <action>
    Update all fields to reflect new milestone:
    - Current focus: v0.5 Housekeeping + Plan Resilience
    - Milestone: v0.5 Housekeeping + Plan Resilience (v0.5.0)
    - Phase: 1 of 2 — Housekeeping v0.5
    - Plan: 05-01 — Housekeeping v0.5
    - Status: PLAN created
    - Last activity: 2026-03-25 — Plan created for phase 05
    - Progress: Milestone 0%, Phase 0%
    - Loop position: PLAN ✓ → APPLY ○ → UNIFY ○
    - Clear Last Execution section (fresh milestone)
    - Session Continuity: next action → /teddy:apply
  </action>
  <verify>grep "v0.5" .teddy/STATE.md && grep "Phase.*05" .teddy/STATE.md</verify>
  <done>AC-3 satisfied: STATE.md reflects v0.5 current position</done>
</task>

<task type="auto" role="state-sync" wave="1">
  <name>Task 4: Validate cross-file version consistency</name>
  <files>.teddy/PROJECT.md, .teddy/ROADMAP.md, .teddy/STATE.md, package.json</files>
  <action>
    Read all four files and verify:
    - package.json version: 0.5.0
    - PROJECT.md version: 0.5.0
    - ROADMAP.md current milestone: v0.5
    - STATE.md milestone: v0.5
    If any mismatch found, fix it.
  </action>
  <verify>grep '"version": "0.5.0"' package.json && grep "0.5.0" .teddy/PROJECT.md && grep "v0.5" .teddy/ROADMAP.md && grep "v0.5" .teddy/STATE.md</verify>
  <done>AC-1 satisfied: All files consistently show v0.5.0</done>
</task>

<!-- docs-dev tasks -->
<task type="auto" role="docs-dev" wave="1">
  <name>Task 5: Add /teddy:cleanup to README commands table</name>
  <files>README.md</files>
  <action>
    Add a row to the Commands table in README.md for /teddy:cleanup:
    | `/teddy:cleanup` | Clean orphaned teams, tasks, and worktrees |

    Insert it after /teddy:review (cleanup is a maintenance command, place it after the main workflow commands).

    Also verify the description matches what's in teddy.md:
    "Detect and clean orphaned teams/tasks/worktrees"
  </action>
  <verify>grep "cleanup" README.md</verify>
  <done>AC-4 satisfied: /teddy:cleanup listed in README</done>
</task>

<task type="auto" role="docs-dev" wave="1">
  <name>Task 6: Verify all 11 commands present in README</name>
  <files>README.md</files>
  <action>
    Cross-reference README commands table against teddy.md commands section.
    All 11 commands must be listed:
    1. /teddy:init
    2. /teddy:explore
    3. /teddy:plan
    4. /teddy:apply
    5. /teddy:unify
    6. /teddy:status
    7. /teddy:resume
    8. /teddy:debug
    9. /teddy:review
    10. /teddy:cleanup
    11. /teddy:map-codebase

    If any are missing or have mismatched descriptions, fix them.
  </action>
  <verify>for cmd in init explore plan apply unify status resume debug review cleanup map-codebase; do grep -q "$cmd" README.md || echo "MISSING: $cmd"; done</verify>
  <done>AC-4 satisfied: All 11 commands in README match teddy.md</done>
</task>

<!-- deps-eng tasks -->
<task type="auto" role="deps-eng" wave="1">
  <name>Task 7: Bump package.json version to 0.5.0</name>
  <files>package.json</files>
  <action>
    Update "version" field in package.json from "0.4.0" to "0.5.0".
  </action>
  <verify>grep '"version": "0.5.0"' package.json</verify>
  <done>AC-1 partially satisfied: package.json shows v0.5.0</done>
</task>

<task type="auto" role="deps-eng" wave="1">
  <name>Task 8: Upgrade vitest from 3.x to 4.x</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run: npm install vitest@^4.0.0 --save-dev
    This updates both package.json and package-lock.json.
    If the install fails (peer dep conflicts), try vitest@latest instead.
  </action>
  <verify>grep '"vitest"' package.json | grep '"\\^4'</verify>
  <done>AC-5 partially satisfied: vitest 4.x installed</done>
</task>

<task type="auto" role="deps-eng" wave="1">
  <name>Task 9: Run test suite with vitest 4.x</name>
  <files>package.json</files>
  <action>
    Run: npm test
    Expect all tests to pass.
    If any tests fail due to vitest 4.x breaking changes:
    - Check vitest 4.x migration guide
    - Fix test files (test/commands.test.js, test/install.test.js)
    - Fix vitest.config.js if needed
    If deprecation warnings appear, address them.
  </action>
  <verify>npm test 2>&1 | tail -5</verify>
  <done>AC-5 satisfied: All tests pass with vitest 4.x</done>
</task>

## Boundaries

<boundaries>

## DO NOT CHANGE
- src/commands/* (no command files modified in this phase)
- src/frameworks/* (no framework files modified)
- src/templates/* (no template files modified)
- bin/install.js (installer untouched)
- test/* (unless vitest 4.x requires migration fixes)

## SCOPE LIMITS
- This plan is housekeeping only — no new features
- Do not add new commands or modify existing command behavior
- Do not change the installer or skill architecture
- vitest upgrade: fix breakage only, do not add new tests

## ROLE ISOLATION
- state-sync owns: .teddy/PROJECT.md, .teddy/STATE.md, .teddy/ROADMAP.md
- docs-dev owns: README.md
- deps-eng owns: package.json, package-lock.json
- No cross-ownership modifications

</boundaries>

## Verification

Before declaring plan complete:
- [ ] `grep "0.5.0" .teddy/PROJECT.md` — version synced
- [ ] `grep "v0.5" .teddy/ROADMAP.md` — milestone exists
- [ ] `grep "v0.5" .teddy/STATE.md` — state updated
- [ ] `grep '"version": "0.5.0"' package.json` — version bumped
- [ ] `grep "cleanup" README.md` — command listed
- [ ] `npm test` — all tests pass with vitest 4.x

## Success Criteria

- All state files consistently reference v0.5.0
- package.json bumped to 0.5.0
- ROADMAP defines v0.5 milestone with phases 05 and 06
- STATE.md ready for phase 05 execution
- README lists all 11 commands
- vitest 4.x installed and all tests pass

## Output

After completion, create `.teddy/phases/05-housekeeping-v05/05-01-SUMMARY.md`
