---
phase: 08-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - test/install.test.js
  - test/commands.test.js
  - src/commands/*.md
  - src/frameworks/*.md
  - src/templates/*.md
  - src/templates/codebase/*.md
  - src/context/session.md
  - src/checklists/*.md
  - README.md
autonomous: true
team:
  name: teddy-08-01
  size: 3
  parallel_waves: 1
  roles:
    - name: installer-dev
      domain: "Installer bug fix and test coverage for install.js"
      owns: ["bin/install.js", "test/install.test.js", "test/commands.test.js"]
    - name: commands-reviewer
      domain: "Review and fix all command markdown files"
      owns: ["src/commands/"]
    - name: docs-reviewer
      domain: "Review and fix framework, template, context, checklist, and README files"
      owns: ["src/frameworks/", "src/templates/", "src/context/", "src/checklists/", "README.md"]
---

<objective>
## Goal
Fix install.js bug (3 missing commands in help output), add test parity for install listing vs src/commands/, and review all markdown files fixing formatting, typos, and inconsistencies.

## Purpose
Polish the framework before growing — users must see all available commands after install, tests must catch listing drift, and documentation must be clean and consistent.

## Output
- `bin/install.js` with complete command listing
- New parity test ensuring install.js stays in sync with src/commands/
- All markdown files reviewed and fixed for formatting, typos, and consistency
</objective>

<context>
## Project Context
@.teddy/PROJECT.md
@.teddy/ROADMAP.md
@.teddy/STATE.md

## Exploration
@.teddy/explorations/EXPLORATION-2026-03-25-polish-automation.md

## Source Files
@bin/install.js
@test/install.test.js
@test/commands.test.js
@src/commands/teddy.md
@README.md
</context>

<acceptance_criteria>

## AC-1: Install listing shows all commands
```gherkin
Given a user runs `npx teddy-framework --global`
When the install completes
Then the console output lists ALL commands from src/commands/ including amend-plan, rollback, and flows
```

## AC-2: Parity test catches listing drift
```gherkin
Given src/commands/ contains N command files
When the parity test runs
Then it verifies that install.js console output references all N commands (minus teddy.md router)
And the test fails if any command is missing from the listing
```

## AC-3: All tests pass
```gherkin
Given all code changes are complete
When `npx vitest run` executes
Then all tests pass (zero failures)
```

## AC-4: Markdown consistency
```gherkin
Given all markdown files in src/ and README.md
When reviewed for formatting, typos, and terminology
Then all files follow consistent patterns:
  - Correct Portuguese and English spelling
  - Consistent terminology ("teammates" not mixed with "agents" randomly)
  - Consistent blank lines around headers and code blocks
  - Consistent table formatting
  - No typos or broken formatting
```

</acceptance_criteria>

<tasks>

<!-- installer-dev tasks (5 tasks) -->
<task type="auto" role="installer-dev" wave="1">
  <name>Task 1: Add missing commands to install.js console listing</name>
  <files>bin/install.js</files>
  <action>
    In the `install()` function, the console.log block (lines 112-123) lists available commands
    but is missing 3 commands added in v0.5/v0.6. Add these lines in alphabetical order
    within the existing listing:

    Add after the `/teddy:apply` line:
      console.log(`    /teddy:amend-plan    Modify active plan mid-execution`);

    Add after the `/teddy:debug` line:
      console.log(`    /teddy:flows         Manage skill dependencies`);

    Add after the `/teddy:resume` line:
      console.log(`    /teddy:review        Code review`);
      console.log(`    /teddy:rollback      Revert UNIFY to pre-merge state`);

    Wait — `/teddy:review` is already listed at line 123. Check the current listing and only
    add what's actually missing. The 3 missing commands are: amend-plan, rollback, flows.

    Maintain the same formatting pattern: aligned descriptions using spaces.
  </action>
  <verify>Run `node bin/install.js --help` and visually confirm all commands are listed. Count should be 14 commands (all files in src/commands/ minus teddy.md).</verify>
  <done>AC-1 satisfied: install output lists amend-plan, rollback, and flows</done>
</task>

<task type="auto" role="installer-dev" wave="1">
  <name>Task 2: Add install listing parity test</name>
  <files>test/install.test.js</files>
  <action>
    Add a new test section in test/install.test.js that verifies the install() function's
    console output lists all commands from src/commands/.

    Strategy:
    1. Read all .md filenames from src/commands/ (excluding teddy.md which is the router)
    2. For each file, derive the command name: `teddy:${basename}` (e.g., "teddy:apply")
    3. Read the install.js source code as a string
    4. Verify the source contains each `/teddy:{command}` string

    Example test:
    ```javascript
    describe("install command listing parity", () => {
      it("install.js source lists all command files from src/commands/ (except teddy.md router)", () => {
        const installSrc = fs.readFileSync(
          path.join(__dirname, "..", "bin", "install.js"),
          "utf8"
        );

        const commandFiles = fs.readdirSync(
          path.join(__dirname, "..", "src", "commands")
        ).filter(f => f.endsWith(".md") && f !== "teddy.md");

        for (const file of commandFiles) {
          const cmdName = `teddy:${file.replace(".md", "")}`;
          expect(installSrc).toContain(`/teddy:${file.replace(".md", "")}`);
        }
      });
    });
    ```

    This test will fail in the future if a new command is added to src/commands/
    but not to install.js listing.
  </action>
  <verify>Run `npx vitest run test/install.test.js` — new parity test passes.</verify>
  <done>AC-2 satisfied: parity test exists and catches drift</done>
</task>

<task type="auto" role="installer-dev" wave="1">
  <name>Task 3: Update commands.test.js minimum count</name>
  <files>test/commands.test.js</files>
  <action>
    In test/commands.test.js line 15, the assertion checks:
      `expect(commandFiles.length).toBeGreaterThanOrEqual(11)`

    Update 11 to 14 to reflect the current number of command files (15 files including teddy.md,
    but the test counts all .md files, so >=14 is conservative and correct for 15 files).

    Actually, check: the test reads ALL .md files in src/commands/ including teddy.md.
    So the count should be >=15 (currently there are exactly 15 files).
    Use >=14 to be slightly conservative while still catching accidental mass deletions.
  </action>
  <verify>Run `npx vitest run test/commands.test.js` — updated assertion passes.</verify>
  <done>AC-3 partial: test assertions updated</done>
</task>

<task type="auto" role="installer-dev" wave="1">
  <name>Task 4: Verify install.js command order and formatting</name>
  <files>bin/install.js</files>
  <action>
    After adding the 3 missing commands, review the entire command listing block in install.js
    to ensure:
    1. Commands are in a logical order (workflow order: init, explore, plan, apply, amend-plan,
       unify, rollback, status, resume, cleanup, debug, review, map-codebase, flows)
    2. Description padding is consistent (all descriptions start at the same column)
    3. No duplicate entries

    Reorder if needed to match the logical workflow order above.
  </action>
  <verify>Read the listing block and confirm order is logical and formatting is aligned.</verify>
  <done>AC-1 satisfied: listing is complete, ordered, and formatted</done>
</task>

<task type="auto" role="installer-dev" wave="1">
  <name>Task 5: Run full test suite and verify green</name>
  <files>test/install.test.js, test/commands.test.js</files>
  <action>
    Run `npx vitest run` to execute all tests.
    Verify all tests pass including the new parity test.
    If any test fails, fix the issue (likely a count or listing mismatch).
  </action>
  <verify>Run `npx vitest run` — output shows all tests passing, zero failures.</verify>
  <done>AC-3 satisfied: all tests pass</done>
</task>

<!-- commands-reviewer tasks (5 tasks) -->
<task type="auto" role="commands-reviewer" wave="1">
  <name>Task 6: Review and fix teddy.md, init.md, explore.md</name>
  <files>src/commands/teddy.md, src/commands/init.md, src/commands/explore.md</files>
  <action>
    Read each file completely and fix:
    1. **Typos** — Portuguese and English spelling errors
    2. **Formatting** — Ensure consistent blank lines before ## headers, proper code block
       language tags (```bash, ```yaml, ```markdown, etc.), consistent table column alignment
    3. **Terminology** — Use "teammates" consistently when referring to Agent Team members
       working in isolated worktrees. Use "agents" or "subagents" only when referring to
       Agent tool spawns that are NOT part of a Team.
    4. **Consistency** — Check that all XML tags that are opened are also closed
       (e.g., `<purpose>...</purpose>`, `<steps>...</steps>`, `<step>...</step>`)

    DO NOT change the semantic content, instructions, or behavior of the skills.
    Only fix formatting, typos, and consistency issues.
  </action>
  <verify>Read each file and confirm no typos, consistent formatting, and proper tag structure.</verify>
  <done>AC-4 partial: 3/15 command files reviewed and fixed</done>
</task>

<task type="auto" role="commands-reviewer" wave="1">
  <name>Task 7: Review and fix plan.md, apply.md, unify.md</name>
  <files>src/commands/plan.md, src/commands/apply.md, src/commands/unify.md</files>
  <action>
    Same review criteria as Task 6:
    1. Fix typos (Portuguese and English)
    2. Fix formatting (blank lines, code blocks, tables)
    3. Standardize terminology (teammates vs agents)
    4. Ensure XML tags are properly opened and closed

    These are the 3 largest command files (242-356 lines each). Be thorough but
    only fix actual issues — do not rewrite or restructure.
  </action>
  <verify>Read each file and confirm no typos, consistent formatting, and proper tag structure.</verify>
  <done>AC-4 partial: 6/15 command files reviewed and fixed</done>
</task>

<task type="auto" role="commands-reviewer" wave="1">
  <name>Task 8: Review and fix status.md, resume.md, cleanup.md</name>
  <files>src/commands/status.md, src/commands/resume.md, src/commands/cleanup.md</files>
  <action>
    Same review criteria as Task 6:
    1. Fix typos (Portuguese and English)
    2. Fix formatting (blank lines, code blocks, tables)
    3. Standardize terminology
    4. Ensure XML tags are properly opened and closed
  </action>
  <verify>Read each file and confirm no typos, consistent formatting, and proper tag structure.</verify>
  <done>AC-4 partial: 9/15 command files reviewed and fixed</done>
</task>

<task type="auto" role="commands-reviewer" wave="1">
  <name>Task 9: Review and fix debug.md, review.md, map-codebase.md</name>
  <files>src/commands/debug.md, src/commands/review.md, src/commands/map-codebase.md</files>
  <action>
    Same review criteria as Task 6:
    1. Fix typos (Portuguese and English)
    2. Fix formatting (blank lines, code blocks, tables)
    3. Standardize terminology
    4. Ensure XML tags are properly opened and closed
  </action>
  <verify>Read each file and confirm no typos, consistent formatting, and proper tag structure.</verify>
  <done>AC-4 partial: 12/15 command files reviewed and fixed</done>
</task>

<task type="auto" role="commands-reviewer" wave="1">
  <name>Task 10: Review and fix amend-plan.md, rollback.md, flows.md</name>
  <files>src/commands/amend-plan.md, src/commands/rollback.md, src/commands/flows.md</files>
  <action>
    Same review criteria as Task 6:
    1. Fix typos (Portuguese and English)
    2. Fix formatting (blank lines, code blocks, tables)
    3. Standardize terminology
    4. Ensure XML tags are properly opened and closed

    Note: flows.md already closes XML tags properly — use it as reference pattern
    for the other files.
  </action>
  <verify>Read each file and confirm no typos, consistent formatting, and proper tag structure.</verify>
  <done>AC-4 partial: 15/15 command files reviewed and fixed</done>
</task>

<!-- docs-reviewer tasks (5 tasks) -->
<task type="auto" role="docs-reviewer" wave="1">
  <name>Task 11: Review and fix src/frameworks/*.md</name>
  <files>src/frameworks/teammate-orchestration.md, src/frameworks/plan-format.md, src/frameworks/quality-principles.md, src/frameworks/loop-phases.md, src/frameworks/skill-flows.md</files>
  <action>
    Read each of the 5 framework files and fix:
    1. **Typos** — Portuguese and English spelling errors
    2. **Formatting** — Consistent blank lines, code block language tags, table alignment
    3. **Terminology** — "teammates" for Agent Team members, "agents/subagents" for standalone spawns
    4. **Structure** — Framework files use wrapper XML tags (e.g., `<plan_format>...</plan_format>`).
       Ensure these wrapper tags are consistent: opened at line 1, closed at last line.

    DO NOT change the semantic content or instructions. Only fix formatting and typos.
  </action>
  <verify>Read each file and confirm no typos, consistent formatting.</verify>
  <done>AC-4 partial: 5 framework files reviewed and fixed</done>
</task>

<task type="auto" role="docs-reviewer" wave="1">
  <name>Task 12: Review and fix src/templates/*.md (non-codebase)</name>
  <files>src/templates/EXPLORATION.md, src/templates/PLAN.md, src/templates/PROJECT.md, src/templates/ROADMAP.md, src/templates/STATE.md, src/templates/SUMMARY.md, src/templates/FLOWS.md</files>
  <action>
    Read each of the 7 template files and fix:
    1. **Typos** — Portuguese and English spelling
    2. **Formatting** — Consistent blank lines, table formatting, placeholder syntax
    3. **Consistency** — Ensure all templates follow the same structural pattern
       (header, description, field documentation)

    DO NOT change template structure or placeholder variables.
  </action>
  <verify>Read each file and confirm no typos, consistent formatting.</verify>
  <done>AC-4 partial: 7 template files reviewed and fixed</done>
</task>

<task type="auto" role="docs-reviewer" wave="1">
  <name>Task 13: Review and fix src/templates/codebase/*.md</name>
  <files>src/templates/codebase/STACK.md, src/templates/codebase/ARCHITECTURE.md, src/templates/codebase/STRUCTURE.md, src/templates/codebase/CONVENTIONS.md, src/templates/codebase/TESTING.md, src/templates/codebase/INTEGRATIONS.md, src/templates/codebase/CONCERNS.md</files>
  <action>
    Read each of the 7 codebase template files and fix:
    1. **Typos** — Portuguese and English spelling
    2. **Formatting** — Consistent blank lines, table formatting, placeholder syntax
    3. **Consistency** — Ensure all 7 codebase templates follow the same structural pattern
  </action>
  <verify>Read each file and confirm no typos, consistent formatting.</verify>
  <done>AC-4 partial: 7 codebase template files reviewed and fixed</done>
</task>

<task type="auto" role="docs-reviewer" wave="1">
  <name>Task 14: Review and fix README.md</name>
  <files>README.md</files>
  <action>
    Read README.md and fix:
    1. **Typos** — Check all English text
    2. **Command table** — Verify it lists all 14 commands (should already include amend-plan,
       rollback, flows from v0.5/v0.6)
    3. **Formatting** — Code blocks, links, section structure
    4. **Accuracy** — Verify the "Project Structure" and "Framework Files" sections match
       the actual directory layout
  </action>
  <verify>Read README.md and confirm accuracy, no typos, consistent formatting.</verify>
  <done>AC-4 partial: README reviewed and fixed</done>
</task>

<task type="auto" role="docs-reviewer" wave="1">
  <name>Task 15: Review and fix context and checklist files</name>
  <files>src/context/session.md, src/checklists/plan-review.md, src/checklists/unify-gate.md</files>
  <action>
    Read each of the 3 files and fix:
    1. **Typos** — Portuguese and English spelling
    2. **Formatting** — Consistent checkbox syntax (`- [ ]`), table alignment, blank lines
    3. **Consistency** — Both checklists should follow the same structural pattern
  </action>
  <verify>Read each file and confirm no typos, consistent formatting.</verify>
  <done>AC-4 satisfied: all markdown files reviewed and fixed</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- .teddy/ state files (PROJECT.md, ROADMAP.md, STATE.md) — managed by framework
- .teddy/phases/ — historical plan and summary files
- .teddy/explorations/ — exploration outputs
- package.json — version bump will be done separately
- node_modules/

## SCOPE LIMITS
- Fix formatting, typos, and consistency ONLY
- Do NOT change semantic content, instructions, or behavior of skills
- Do NOT add new features or restructure files
- Do NOT modify the logic of install.js beyond the help listing
- Do NOT change how XML tags function — only ensure they are consistently opened and closed

## ROLE ISOLATION (enforced by team.roles.owns)
- installer-dev owns: bin/install.js, test/install.test.js, test/commands.test.js
- commands-reviewer owns: src/commands/
- docs-reviewer owns: src/frameworks/, src/templates/, src/context/, src/checklists/, README.md
- No cross-ownership modifications without SendMessage coordination

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` passes all tests (including new parity test)
- [ ] `node bin/install.js --help` shows all 14 commands
- [ ] All markdown files reviewed for typos and formatting
- [ ] No semantic changes to skill behavior
</verification>

<success_criteria>
- All 3 role-specialists completed their tasks
- All verification checks pass
- All worktrees ready for merge
- install.js lists all commands
- Parity test prevents future listing drift
- Markdown files are clean and consistent
</success_criteria>

<output>
After completion, create `.teddy/phases/08-polish/08-01-SUMMARY.md`
</output>
