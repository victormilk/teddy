---
phase: 02-fix-contradictions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frameworks/quality-principles.md
  - src/checklists/plan-review.md
  - src/commands/teddy.md
  - .teddy/PROJECT.md
  - .teddy/codebase/CONCERNS.md
autonomous: true
team:
  name: teddy-02-01
  size: 2
  parallel_waves: 1
  execution_mode: subagents
  roles:
    - name: src-editor
      domain: "Framework docs, checklists, and command definitions"
      owns: ["src/frameworks/quality-principles.md", "src/checklists/plan-review.md", "src/commands/teddy.md"]
    - name: state-editor
      domain: "Project state files and codebase analysis docs"
      owns: [".teddy/PROJECT.md", ".teddy/codebase/CONCERNS.md"]
---

<objective>
## Goal
Eliminate internal contradictions between Teddy documents — sizing guidance, version numbers, and concern tracking — so that plans created following one document don't fail validation by another.

## Purpose
The framework can't be used with confidence when plan-review.md rejects plans that follow teammate-orchestration.md guidance. Fixing this unblocks real-world usage of the PLAN → APPLY → UNIFY loop.

## Output
- Consistent sizing guidance across all documents (aligned with teammate-orchestration.md as source of truth)
- PROJECT.md version aligned with package.json (v0.3.0)
- CONCERNS.md updated to reflect resolved issues
</objective>

<context>
## Project Context
@.teddy/PROJECT.md
@.teddy/ROADMAP.md
@.teddy/explorations/EXPLORATION-2026-03-24-stabilization.md

## Source of Truth for Sizing
@src/frameworks/teammate-orchestration.md (authoritative — most detailed, correct model)
@src/frameworks/plan-format.md (already aligned with teammate-orchestration)
</context>

<acceptance_criteria>

## AC-1: Sizing Guidance Consistent Across All Documents
```gherkin
Given quality-principles.md, plan-review.md, teddy.md, and teammate-orchestration.md
When I grep for task sizing guidance in each
Then all documents agree on the same model:
  - Agent Teams: 3-5 roles, 5-6 tasks/role, 15-30 total
  - Subagents: 1-3 agents, 1-2 tasks each, <12 total
  - Direct execution: <3 tasks, single domain
And zero references to "2-3 tasks per plan maximum" remain
```

## AC-2: Version Consistent Between PROJECT.md and package.json
```gherkin
Given PROJECT.md and package.json
When I check the version field in each
Then both show v0.3.0
```

## AC-3: Resolved Concerns Marked in CONCERNS.md
```gherkin
Given CONCERNS.md
When I check entries for "Conflicting Task Sizing" and "Plan-Review Checklist Contradictions"
Then both are marked as resolved with date and resolution description
```

</acceptance_criteria>

<tasks>

<!-- src-editor tasks -->
<task type="auto" role="src-editor" wave="1">
  <name>Task 1: Align quality-principles.md sizing with teams model</name>
  <files>src/frameworks/quality-principles.md</files>
  <action>
    Replace the "Scope Control" section (lines 54-66) to reflect the execution spectrum:
    - Agent Teams: 3-5 roles, 5-6 tasks/role, target <50% context per teammate
    - Subagents: 1-3 agents for focused tasks, <12 total tasks
    - Direct execution: <3 tasks, single domain, tight coupling

    Replace the "Teammate Sizing Sweet Spot" table (lines 67-77) to align with
    teammate-orchestration.md sizing guide:
    - Small feature: 3 roles, 4-5 tasks/role, 12-15 total
    - Medium feature: 3-4 roles, 5-6 tasks/role, 15-24 total
    - Large feature: 4-5 roles, 5-6 tasks/role, 20-30 total

    Remove "2-3 tasks per plan maximum" and "Each teammate gets 1 self-contained task".
    Replace with guidance that matches the execution mode to scope.
    Keep the context degradation curve (0-30% peak, 70%+ poor) — that's correct.
  </action>
  <verify>
    grep -n "2-3 tasks" src/frameworks/quality-principles.md returns 0 matches.
    grep -n "5-6 tasks" src/frameworks/quality-principles.md returns matches in sizing table.
  </verify>
  <done>AC-1: quality-principles.md aligned with teams model</done>
</task>

<task type="auto" role="src-editor" wave="1">
  <name>Task 2: Fix plan-review.md scope checklist</name>
  <files>src/checklists/plan-review.md</files>
  <action>
    Replace line 27 "Plan has 2-3 tasks maximum" with scope validation
    that matches the execution spectrum:

    Replace the entire "## Scope" section with:
    - [ ] Plan sizing matches execution mode:
          - Agent Teams: 3-5 roles, 5-6 tasks per role (15-30 total)
          - Subagents: 1-3 agents, 1-2 tasks each (<12 total)
          - Direct: <3 tasks, single domain
    - [ ] Estimated context per teammate is under 50%
    - [ ] Vertical slices preferred over horizontal layers
    - [ ] No single task exceeds 60 minutes estimated effort
  </action>
  <verify>
    grep -n "2-3 tasks" src/checklists/plan-review.md returns 0 matches.
    The "Scope" section contains execution mode tiers.
  </verify>
  <done>AC-1: plan-review.md aligned with teams model</done>
</task>

<task type="auto" role="src-editor" wave="1">
  <name>Task 3: Fix teddy.md expertise description</name>
  <files>src/commands/teddy.md</files>
  <action>
    In the persona/expertise section, replace:
      "Work decomposition into atomic units (2-3 tasks per plan)"
    with:
      "Work decomposition into atomic units with execution mode selection (direct, subagents, or Agent Teams)"

    This removes the incorrect "2-3 tasks" reference while keeping the
    decomposition concept accurate.
  </action>
  <verify>
    grep -n "2-3 tasks" src/commands/teddy.md returns 0 matches.
  </verify>
  <done>AC-1: teddy.md aligned with teams model</done>
</task>

<!-- state-editor tasks -->
<task type="auto" role="state-editor" wave="1">
  <name>Task 4: Update PROJECT.md version to v0.3.0</name>
  <files>.teddy/PROJECT.md</files>
  <action>
    In the "Current State" table, change:
      | Version | 0.2.0 |
    to:
      | Version | 0.3.0 |

    This aligns with package.json which already shows 0.3.0.
  </action>
  <verify>
    grep "0.2.0" .teddy/PROJECT.md returns 0 matches.
    grep "0.3.0" .teddy/PROJECT.md returns 1 match in the version row.
  </verify>
  <done>AC-2: PROJECT.md version matches package.json</done>
</task>

<task type="auto" role="state-editor" wave="1">
  <name>Task 5: Mark resolved concerns in CONCERNS.md</name>
  <files>.teddy/codebase/CONCERNS.md</files>
  <action>
    Under "## Tech Debt", update "Conflicting Task Sizing Guidance" entry:
    - Add "**Status: RESOLVED (2026-03-24)**" at the start
    - Change "Fix approach" to "Resolution: Consolidated to execution spectrum model in quality-principles.md, plan-review.md, and teddy.md (Phase 02)"

    Under "## Known Bugs", update "Plan-Review Checklist Contradictions" entry:
    - Add "**Status: RESOLVED (2026-03-24)**"
    - Change description to note fix in Phase 02
  </action>
  <verify>
    grep "RESOLVED" .teddy/codebase/CONCERNS.md returns 2 matches.
  </verify>
  <done>AC-3: Resolved concerns marked in CONCERNS.md</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/frameworks/teammate-orchestration.md (source of truth — already correct)
- src/frameworks/plan-format.md (already aligned)
- src/frameworks/loop-phases.md (not related to sizing)
- bin/install.js (Phase 03 scope)
- Any command files other than teddy.md

## SCOPE LIMITS
- Fix contradictions only — no new features or restructuring
- Do not change the teammate-orchestration.md model, align others TO it
- Do not add new sections to any document
- Routing standardization (finding #7) deprioritized — on inspection, all commands already wait for user input; the inconsistency is minor wording, not behavioral

## ROLE ISOLATION
- src-editor owns: src/frameworks/quality-principles.md, src/checklists/plan-review.md, src/commands/teddy.md
- state-editor owns: .teddy/PROJECT.md, .teddy/codebase/CONCERNS.md
- No overlapping files

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep -rn "2-3 tasks" src/ returns 0 matches (excluding templates that reference the old pattern as examples)
- [ ] grep "0.2.0" .teddy/PROJECT.md returns 0 matches
- [ ] grep "RESOLVED" .teddy/codebase/CONCERNS.md returns 2+ matches
- [ ] All 3 ACs satisfied
</verification>

<success_criteria>
- All 5 tasks completed
- All verification checks pass
- Zero sizing contradictions remain across framework documents
- PROJECT.md version matches package.json
- CONCERNS.md reflects resolved state
</success_criteria>

<output>
After completion, create `.teddy/phases/02-fix-contradictions/02-01-SUMMARY.md`
</output>
